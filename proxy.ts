/**
 * Este archivo fue el middleware global bajo el nombre incorrecto "proxy.ts".
 * Next.js solo reconoce "middleware.ts" — este archivo nunca fue ejecutado.
 *
 * La lógica ha sido movida a /middleware.ts (raíz del proyecto).
 * Este archivo puede eliminarse de forma segura.
 *
 * @deprecated Reemplazado por middleware.ts
 */
